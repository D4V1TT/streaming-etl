[project]
name = "streaming-etl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Davit Tavadze", email = "davitit9@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
streaming-etl = "streaming_etl:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "ruff>=0.16.6",
]
